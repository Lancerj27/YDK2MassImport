Just a simple python script I converted to a website. This uses API requests to ygoprodeck to convert a .ydk which is used by programs like ignis and turns them into text that can be mass imported into TCGplayer. 

This code is AI generated. 
